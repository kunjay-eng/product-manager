ถ้าเคยรันไปแล้ว **ระบบสำรองข้อมูลของระบบหุ้นทำงานอัตโนมัติอยู่แล้วตอนนี้ ไม่ต้องทำอะไรเพิ่มเลยครับ** — สำรอง 2 ไฟล์ (Stock_Database, STOCK_PRICE_DATABAS) ทุกวันตอน 03:30 อัตโนมัติ พร้อมแจ้งเตือน Telegram และล้างไฟล์เก่าเกิน 30 วันให้เอง

สิ่งเดียวที่ยังไม่มีคือ**เมนูกดสำรองมือ**ในตัว Google Sheet เอง (เหมือนที่ทำให้ระบบสินค้า) อยากได้ไหมครับ? ถ้าอยาก นี่คือโค้ดที่ตรงกับชื่อฟังก์ชันจริงของระบบหุ้น (ไม่ชนกับอะไรเลย เพราะไม่มี `onOpen()` อยู่ก่อน):

```js
function onOpen(){
  SpreadsheetApp.getUi()
    .createMenu('💾 สำรองข้อมูล')
    .addItem('สำรองตอนนี้', 'testBackupNow')
    .addToUi();
}
```

วางไว้ในไฟล์ไหนก็ได้ในโปรเจกต์หุ้น (เช่นต่อท้าย `webapp_00c_helpers.gs`) — กดแล้วจะเรียก `testBackupNow()` ซึ่งไปเรียก `backupAllSheets()` ตัวเดียวกับที่ Trigger รันอัตโนมัติทุกวันอยู่แล้ว ผลลัพธ์เหมือนกันทุกอย่าง แค่กดเองได้ทันทีเวลาต้องการโดยไม่ต้องรอถึง 03:30


ใช่ครับ ตรงตามที่คุยไว้เลย เพิ่มอีกนิดให้กดจากเมนูแล้วเห็นผลทันที (ไม่ต้องรอเช็ค Telegram อย่างเดียว) — แทนที่โค้ดก่อนหน้าด้วยอันนี้:

```js
function onOpen(){
  SpreadsheetApp.getUi()
    .createMenu('💾 สำรองข้อมูล')
    .addItem('สำรองตอนนี้', 'manualBackupWithAlert')
    .addToUi();
}

function manualBackupWithAlert(){
  backupAllSheets();
  SpreadsheetApp.getUi().alert('✅ สำรองข้อมูลเสร็จแล้ว\n\nไฟล์ถูกเก็บไว้ในโฟลเดอร์ Drive: ' + BACKUP_FOLDER_NAME);
}
```

**วางไว้ที่ไหนก็ได้ในโปรเจกต์หุ้น** (เช่นต่อท้าย `webapp_00c_helpers.gs` หรือใกล้ๆ `testBackupNow()`) — ใช้ฟังก์ชัน `backupAllSheets()` ตัวเดียวกับที่ Trigger อัตโนมัติเรียกอยู่ทุกวันตอน 03:30 เป๊ะ ไม่ได้สร้างระบบสำรองใหม่ซ้ำซ้อน แค่เพิ่มทางกดมือ + popup ยืนยันหลังกดเสร็จ ต่างจาก `testBackupNow()` เดิมตรงที่มี alert เด้งบอกผลด้วย ไม่ต้องไปเช็ค Telegram หรือ Logger เอง

Deploy ใหม่ (หรือแค่เซฟถ้าไม่ได้เป็น Web App — โปรเจกต์นี้รันผ่านเมนู Sheet ไม่ต้องผ่าน `/exec` เหมือนระบบสินค้า เซฟแล้วรีเฟรชหน้า Sheet ก็เห็นเมนูใหม่ทันที) ลองกด "💾 สำรองข้อมูล → สำรองตอนนี้" ดูได้เลยครับ