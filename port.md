ถ้าเคยรันไปแล้ว **ระบบสำรองข้อมูลของระบบหุ้นทำงานอัตโนมัติอยู่แล้วตอนนี้ ไม่ต้องทำอะไรเพิ่มเลยครับ** — สำรอง 2 ไฟล์ (Stock_Database, STOCK_PRICE_DATABAS) ทุกวันตอน 03:30 อัตโนมัติ พร้อมแจ้งเตือน Telegram และล้างไฟล์เก่าเกิน 30 วันให้เอง

สิ่งเดียวที่ยังไม่มีคือ**เมนูกดสำรองมือ**ในตัว Google Sheet เอง (เหมือนที่ทำให้ระบบสินค้า) อยากได้ไหมครับ? ถ้าอยาก นี่คือโค้ดที่ตรงกับชื่อฟังก์ชันจริงของระบบหุ้น (ไม่ชนกับอะไรเลย เพราะไม่มี `onOpen()` อยู่ก่อน):

```js
function onOpen(){
  SpreadsheetApp.getUi()
    .createMenu('💾 สำรองข้อมูล')
    .addItem('สำรองตอนนี้', 'testBackupNow')
    .addToUi();
}
```

วางไว้ในไฟล์ไหนก็ได้ในโปรเจกต์หุ้น (เช่นต่อท้าย `webapp_00c_helpers.gs`) — กดแล้วจะเรียก `testBackupNow()` ซึ่งไปเรียก `backupAllSheets()` ตัวเดียวกับที่ Trigger รันอัตโนมัติทุกวันอยู่แล้ว ผลลัพธ์เหมือนกันทุกอย่าง แค่กดเองได้ทันทีเวลาต้องการโดยไม่ต้องรอถึง 03:30